logs/*.log
.DS_Store

# Generated by Admin > Site Settings > SEO & Sitemap — environment-specific
# (bakes in APP_URL), regenerate per deployment instead of committing it.
/sitemap.xml

# User-uploaded content — keep the folder structure, not the files
uploads/avatars/*
uploads/certificates/*
uploads/reports/*
uploads/products/*
uploads/blog/*
uploads/medicines/*
!uploads/avatars/.gitkeep
!uploads/certificates/.gitkeep
!uploads/reports/.gitkeep
!uploads/products/.gitkeep
!uploads/blog/.gitkeep
!uploads/medicines/.gitkeep
!uploads/.htaccess
